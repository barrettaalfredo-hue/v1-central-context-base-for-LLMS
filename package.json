{
  "name": "v1-central-context-base-for-llms",
  "version": "0.1.0",
  "private": true,
  "description": "Privat molnminne för Claude Desktop – TypeScript-monorepo (dashboard, api, memory).",
  "packageManager": "pnpm@10.33.3",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "build": "pnpm -r run build",
    "test": "pnpm -r run test",
    "typecheck": "pnpm -r run typecheck"
  }
}
